import { css } from 'emotion';

export const headerContainerStyle = css`
  position: relative;
  text-align: center;
`;
export const isLiveStyle = css`
  position: absolute;
  top: 8px;
  left: 16px;
  background: var(--color-warning);
  color: white;
  border-radius: 0.2rem;
`;
export const viewsStyle = css`
  position: absolute;
  top: 8px;
  right: 10px;
  background: rgba(0, 0, 0, 0.5);
  color: white;
  border-radius: 0.2rem;
`;
